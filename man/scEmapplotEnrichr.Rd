% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Seurat.Utils.Visualization.R
\name{scEmapplotEnrichr}
\alias{scEmapplotEnrichr}
\title{Enrichment Map (GO term network) by enrichplot}
\usage{
scEmapplotEnrichr(
  df.enrichment,
  showCategory = 20,
  min_edge = 0.2,
  tag = "...",
  universe = df.enrichment@universe,
  title = paste("GO Enrichment Map", tag),
  subtitle = kppws("Input: ", substitute_deparse(df.enrichment)),
  caption = paste0("Input genes: ", length(df.enrichment@gene), " | Enriched terms: ",
    nrow(df.enrichment), " | Shown: ", min(showCategory, nrow(df.enrichment)),
    " | background genes: ", length(universe), " | min edge overlap: ", min_edge),
  layout = "kk",
  cex_label_category = 0.8,
  save = TRUE,
  also.pdf = FALSE,
  save.obj = FALSE,
  w = 10,
  h = 10,
  ...
)
}
\arguments{
\item{df.enrichment}{enrichResult object (e.g. from clusterProfiler::enrichGO).}

\item{showCategory}{Integer. Number of GO terms (nodes) to show. Default: 30.}

\item{min_edge}{Numeric. Minimum similarity (overlap) to draw edges. Default: 0.2.}

\item{tag}{Character. Tag added to the plot title. Default: "...".}

\item{universe}{Character. Background gene list. Default: \code{df.enrichment@universe}.}

\item{title}{Character. Plot title. Default: "GO Enrichment Map" + tag.}

\item{subtitle}{Character. Subtitle. Default: derived from input object.}

\item{caption}{Character. Caption. Default: constructed from input parameters.}

\item{layout}{Character. igraph layout name passed to emapplot. Default: "kk".}

\item{save}{Logical. Whether to save the plot. Default: TRUE.}

\item{also.pdf}{Logical. Save both png and pdf. Default: FALSE.}

\item{save.obj}{Logical. Whether to save the ggplot object. Default: FALSE.}

\item{w}{Width in inches. Default: 10.}

\item{h}{Height in inches. Default: 10.}

\item{...}{Additional arguments passed to \code{enrichplot::emapplot()}.}
}
\value{
A ggplot object (invisibly if saved).
}
\description{
Wrapper around \code{enrichplot::emapplot()} to visualize GO enrichment results
as a network. Nodes are enriched GO terms, edges represent gene overlap.
Includes safety checks, informative fallback plots, and optional saving,
mirroring the behavior of \code{scBarplotEnrichr()}.
}
\examples{
\dontrun{
scEmapplotEnrichr(df.enrichment, tag = "Cluster 3 neurons")
}
}
