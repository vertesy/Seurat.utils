% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Seurat.Utils.Visualization.R
\name{scGeneConceptNetworkEnrichr}
\alias{scGeneConceptNetworkEnrichr}
\title{Gene-Concept Network Plot (cnetplot wrapper)}
\usage{
scGeneConceptNetworkEnrichr(
  df.enrichment,
  showCategory = 10,
  foldChange = NULL,
  tag = NULL,
  title = paste("Gene-Concept Network", tag),
  subtitle = kppws("Input: ", substitute_deparse(df.enrichment)),
  caption = paste0("Enriched terms: ", ifelse(is.null(df.enrichment), 0,
    nrow(df.enrichment)), " | Shown: ", ifelse(is.null(df.enrichment), 0,
    min(showCategory, nrow(df.enrichment))), if (!is.null(foldChange))
    paste0(" | genes w/ foldChange: ", length(foldChange)) else ""),
  circular = FALSE,
  colorEdge = TRUE,
  cex_label_category = 0.8,
  cex_label_gene = cex_label_category,
  node_label = "category",
  save = TRUE,
  also.pdf = FALSE,
  save.obj = FALSE,
  w = 10,
  h = 10,
  ...
)
}
\arguments{
\item{df.enrichment}{enrichResult or gseaResult object.}

\item{showCategory}{Integer. Number of enriched terms to show. Default: 10.}

\item{foldChange}{Named numeric vector of gene-level statistics
(e.g. logFC), names must match gene IDs in enrichment.}

\item{tag}{Character. Tag added to the plot title. Default: "...".}

\item{title}{Character. Plot title. Default: "Gene-Concept Network" + tag.}

\item{subtitle}{Character. Subtitle. Default: derived from input object.}

\item{caption}{Character. Caption. Default: constructed from input parameters.}

\item{circular}{Logical. Draw network in circular layout. Default: FALSE.}

\item{colorEdge}{Logical. Color edges by category. Default: TRUE.}

\item{cex_label_category}{Numeric. Size of category labels. Default: 0.8.}

\item{cex_label_gene}{Numeric. Size of gene labels. Default: same as cex_label_category.}

\item{node_label}{Character. Which nodes to label: "all", "gene",
or "category". Default: "category".}

\item{save}{Logical. Whether to save the plot. Default: TRUE.}

\item{also.pdf}{Logical. Save both png and pdf. Default: FALSE.}

\item{save.obj}{Logical. Whether to save the ggplot object. Default: FALSE.}

\item{w}{Width in inches. Default: 10.}

\item{h}{Height in inches. Default: 10.}

\item{...}{Additional arguments passed to \code{enrichplot::cnetplot()}.}
}
\value{
A ggplot object (invisibly if saved).
}
\description{
Wrapper around \code{enrichplot::cnetplot()} to visualize the gene-concept
(e.g. GO / KEGG) network for enrichment results. The plot shows which genes
drive which enriched terms, optionally colored by fold change (e.g. DE).
Behavior mirrors \code{scBarplotEnrichr()} and \code{scEmapplotEnrichr()} with
safety checks, informative fallbacks, and optional saving.
}
\examples{
\dontrun{
edox <- setReadable(edo, 'org.Hs.eg.db', 'ENTREZID')
scGeneConceptNetworkEnrichr(
  df.enrichment = edox,
  foldChange = geneList,
  tag = "Cluster 3 neurons"
)
}
}
